---

---

<button type="button" id="scrollToTopBtn" class="floating-action-btn fixed right-4 bottom-20 mx-0 hidden" title="scroll to top">
  <i class="ri-arrow-up-line"></i>
</button>

<script>
  const scrollToTopBtn = document.getElementById("scrollToTopBtn");

  if (scrollToTopBtn instanceof HTMLButtonElement) {
    window.addEventListener('scroll', function() {
      if (window.scrollY > 200) {
        scrollToTopBtn.style.display = 'inline-flex';
      } else {
        scrollToTopBtn.style.display = 'none';
      }
    });
    scrollToTopBtn.addEventListener('click', function() {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    });
  }
</script>
